---
title: "ter2"
format: html
editor: visual
---

```{r}
library(ade4)
apropos("dudi")
?doubs
```

```{r}
doubs$xy
table.value(doubs$fish, csize = 0.4)
```

```{r}
data(doubs)
names(doubs)
table.value(doubs$fish, csize =0.4)
```

```{r}
plot(doubs$xy, col = "blue", ty = "l", lwd = 2)
s.label(doubs$xy, add.plot = T)
text(45, 10,"Amont", cex = 2, col = "red")
```

```{r}
s.value(doubs$xy, doubs$fish[, names(doubs$fish) == "Satr"], sub = "Satr", csub = 2)

s.value(doubs$xy, doubs$fish[, names(doubs$fish) == "Satr"], method = "greylevel", sub = "Satr", csub = 2)
```

```{r}

```
